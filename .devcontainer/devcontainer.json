{
  "name": "python-opencv-tesseract-dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "Vue.volar"
      ]
    }
  },
  "postCreateCommand": "pip install -r backend/requirements.txt && cd app && npm install",
  "remoteUser": "root"
}